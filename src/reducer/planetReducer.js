import { GET_PLANETS, GET_PLANETS_ERROR } from "../actions/types";

const initialState = {
  planets: [],
  selectedPlanets: []
};

export default (state = initialState, action) => {
  switch (action.type) {
  }
};
